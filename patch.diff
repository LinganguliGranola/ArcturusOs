--- terminal/commands/DirectoryCommands.c
+++ terminal/commands/DirectoryCommands.c
@@ -142,6 +142,19 @@
         }
         return true;
     }
+    if (starts_with(command, "ls ") || starts_with(command, "ls")) {
+        argument_status = read_optional_argument(command + 2, argument,
+                                                 sizeof(argument));
+        if (argument_status == 1) {
+            if (!filesystem_list(current_path))
+                terminal_writestring("ls: directory not found\n");
+        } else if (argument_status != 2 || !resolve_path(argument, path) || !filesystem_list(path)) {
+            terminal_writestring("ls: directory not found\n");
+        }
+        return true;
+    }
     if (starts_with(command, "cd ") || starts_with(command, "cd")) {
         argument_status = read_optional_argument(command + 2, argument,
                                                  sizeof(argument));
